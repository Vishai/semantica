{
  "name": "semantic-glyph-system",
  "version": "0.1.0",
  "private": true,
  "description": "Semantic Glyph System - Obsidian plugin and mobile apps for symbolic note-taking",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
